{
  "name": "@hauska-engine/engine-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gate-fronted reasoning HTTP service (ADR-008). Sibling to retrieval-api; no reasoning logic until the engine lift lands.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@hauska-engine/adapters": "workspace:*",
    "@hauska-engine/engine-core": "workspace:*",
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
