{
  "name": "@hauska-engine/atom-contract-pin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pre-Sync-1 path-pin shim for @hauska/atom-contract. Mirrors the workspace-private @workspace/empressa-atom contract surface; flips to re-export from @hauska/atom-contract on Sync 1.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
