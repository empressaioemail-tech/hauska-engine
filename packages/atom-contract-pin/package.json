{
  "name": "@hauska-engine/atom-contract-pin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Engine-side re-export shim over @hauska/atom-contract. Originally a pre-Sync-1 path-pin mirroring the workspace-private @workspace/empressa-atom surface; flipped to re-export the published npm package on Sync 1 (cc-agent-AC published @hauska/atom-contract@1.0.0).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@hauska/atom-contract": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
