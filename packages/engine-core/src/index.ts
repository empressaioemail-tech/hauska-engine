/**
 * @hauska-engine/engine-core
 *
 * Scaffold package for ADR-008 engine extraction (sprint 56, step 1).
 * Briefing, finding, hydrology, plan-set decomposition, and precedence
 * engines land here in the lift (steps 3–4), gated behind M-Stabilize 2C.
 */

/** Placeholder export so the skeleton package typechecks and builds. */
export const ENGINE_CORE_SCAFFOLD_VERSION = "0.0.0-scaffold" as const;
