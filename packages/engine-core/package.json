{
  "name": "@hauska-engine/engine-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reasoning engines (briefing, finding, hydrology, decomposition, precedence). Lift target from cortex-api lib/*-engine per ADR-008 / sprint 56.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
