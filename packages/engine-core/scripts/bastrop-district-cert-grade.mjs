#!/usr/bin/env node
/**
 * RETIRED — use block13-cert-grade.mjs (the proven harness).
 *
 *   tsx scripts/block13-cert-grade.mjs --roster-from=query --district-prefix=SF-1
 */
console.error(
  "RETIRED: bastrop-district-cert-grade.mjs — use block13-cert-grade.mjs --roster-from=query --district-prefix=<DIST>",
);
process.exit(2);
