#!/usr/bin/env node
/**
 * RETIRED — use depth-warm-city-batch.mjs with --row-id=Bastrop.
 */
console.error(
  "RETIRED: depth-warm-bastrop-batch.mjs — use depth-warm-city-batch.mjs --row-id=Bastrop",
);
process.exit(2);
