/**
 * @hauska-engine/adapters
 *
 * Scaffold package for ADR-008 engine extraction (sprint 56, step 1).
 * Federal / national / state / local site-context and subsurface
 * adapters lift from cortex-api `lib/adapters` in step 3.
 */

/** Placeholder export so the skeleton package typechecks and builds. */
export const ADAPTERS_SCAFFOLD_VERSION = "0.0.0-scaffold" as const;
