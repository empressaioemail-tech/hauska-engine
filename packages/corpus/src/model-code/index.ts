/**
 * Model-code (Layer 1, ADR-019) — the ICC I-Code model-code base.
 *
 * The structural extractor turns a Code Connect edition into Layer 1
 * `code-edition` / `code-section` / `code-cross-reference` /
 * `code-definition` atoms on the interim deep-link footing.
 */

export * from "./extractor.js";
