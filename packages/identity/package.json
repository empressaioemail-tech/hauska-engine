{
  "name": "@hauska-engine/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Atom identity module per ADR-011. DID resolver, IPNS read/write surfaces, key-custody hooks (custody specifics deferred per ADR-011).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
