{
  "name": "@hauska-engine/capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TCE step 1 — flip live-fetched anticipatory feeds to also persist event-family atoms.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@hauska-engine/atom-contract-pin": "workspace:*",
    "@hauska-engine/identity": "workspace:*",
    "@hauska-engine/storage": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}
