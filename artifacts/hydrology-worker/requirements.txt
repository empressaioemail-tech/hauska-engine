# Pinned upper bounds: pysheds 0.5 (current latest) still calls np.in1d,
# which NumPy 2.x removed — an unpinned resolve (pysheds 0.5 + numpy 2.4)
# imports fine but crashes grid.accumulation with AttributeError at
# runtime, silently forcing the native-D8 fallback on every request.
# Caught by the build-time warm step (warm.py). Keep numpy <2 until
# pysheds ships NumPy-2 compatibility.
pysheds>=0.3.5,<0.6
numpy>=1.26.0,<2.0
scipy>=1.11.0,<1.14
rasterio>=1.3.0,<1.5
