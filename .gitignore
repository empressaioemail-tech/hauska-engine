# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo
.turbo/

# env
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# coverage
coverage/
.nyc_output/

# python worker bytecode (artifacts/*-worker)
__pycache__/
*.pyc

# pipeline-runner local state
services/pipeline-runner/.local/
ingest-cli-state.json

# IPFS dev cache
.ipfs-cache/

# Generated fixtures (kept under git when explicitly added)
*.fixture.local.json
packages/engine-core/src/road-intake/fixtures/bastrop-county-roadway-full.json

# Local recon scratch (downloaded source artifacts + probe scripts)
.tmp-recon/

# Local secret scratch (never commit connection strings)
.dburl.tmp
*.dburl.tmp

# RECIPE-PROOF Caldwell bulk fixtures (local ingest only; too large for git)
packages/engine-core/src/road-intake/fixtures/caldwell-cad-road-centerlines.json
packages/engine-core/src/road-intake/fixtures/caldwell-overpass-lockhart-city-bbox.json
packages/engine-core/caldwell-warm-*.json
packages/engine-core/caldwell-warm-*.txt

# Planner/executor scratch artifacts (2026-08-08 hygiene pass).
# These accumulated across sessions and were repeatedly reported-and-left by
# executors. Ignored so they stop being noise in every git status.
packages/engine-core/_build_*.mjs
packages/engine-core/_tmp_*.mjs
packages/engine-core/_parse_*.mjs
packages/engine-core/depth-warm-*-refused-roster-*.json

# SS-W9 statewide three-layer audit: the full-prose statewide record is multi-megabyte
# and fully regenerable by packages/retrieval/scripts/three-layer-audit.mjs. The rollup and
# the WRITTEN scan cache beside it ARE committed, because they are the evidence.
artifacts/three-layer-audit/statewide.json
